databases:
  - name: mysite3
    databaseName: mysite3
    user: mysite3
    plan: production # 新しいプランに変更

services:
  - type: web
    name: mysite3
    runtime: ruby
    plan: free # 有料プランに変更
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec rails server"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mysite3
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
      - key: WEB_CONCURRENCY
        value: 2 # sensible default